pipeline {
    agent any
    stages {
        stage('Clone Code') {
            steps {
                git branch: 'main',
                    url: 'https://github.com/ankandhang/static-page-in-jenkins.git'
            }
        }
    }
}
